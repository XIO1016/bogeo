import 'package:flutter/material.dart';

class EatPillPage extends StatelessWidget {
  const EatPillPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.white,
    );
  }
}
